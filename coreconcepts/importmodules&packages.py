#modules are used to divide our projects into different files
#each file is considered a module, which can be reused.


import modulename
modulename.function()

#to call a function only:

from modulename import function

import ecommerce.shipping
from ecommerce import shipping


