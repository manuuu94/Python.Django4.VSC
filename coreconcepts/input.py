#input asks for a value in the console before exiting the program, which can be then saved in a variable "name"
#the value input returns is a string, which if necessary needs to be converted

#name = input('What is your name? ')
#print('Hi ' + name)

#expression is a piece of code that uses a value.
#ask two questions: persons name and favourite color. then print a message including both variables.
name = input('name? ')
color = input('favourite color? ')
print(name+' likes '+color)
